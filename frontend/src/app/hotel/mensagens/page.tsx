"use client";
export const dynamic = "force-dynamic";
import HotelPlaceholder from "@/components/HotelPlaceholder";

export default function Page() {
  return <HotelPlaceholder activePage="mensagens" titulo="Mensagens" descricao="Templates e respostas automáticas usadas pela IA." />;
}
